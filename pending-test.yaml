apiVersion: v1
kind: Pod
metadata:
  name: pending-test
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sleep", "3600"]
    resources:
      requests:
        cpu: "100"
        memory: "100Gi"
